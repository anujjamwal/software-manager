<%= form_for(@software) do |f| %>
  <% if @software.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@software.errors.count, "error") %> prohibited this software from being saved:</h2>

      <ul>
      <% @software.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :path %><br>
    <%= f.text_field :path %>
  </div>
  <div class="field">
    <%= f.label :operating_system_id %><br>
    <%= f.number_field :operating_system_id %>
  </div>
  <div class="field">
    <%= f.label :download_policy_id %><br>
    <%= f.collection_select(:download_policy_id, DownloadPolicy.all, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :state %><br>
    <%= f.select(:state, [:approved, :unapproved]) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
